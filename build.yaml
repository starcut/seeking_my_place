targets:
  $default:
    builders:
      freezed:
        options:
          build_extensions:
            .dart:
              - .freezed.dart
      json_serializable:
        options:
          explicit_to_json: true
          include_if_null: false
